{# {% extends 'admin/base_admin.html.twig' %} #}


{% extends 'base.html.twig' %}

{% block title %}Commentaires{% endblock %}

{% block body %}
    <h1>Commentaires</h1>
<br>
    <table class="table">
        <tbody>
         {% for commentaire in commentaires %}
    {# TODO Montrer titre de l'article sur laquel le commentaire est laissé #}

              <tr>
                <th></th>
                <td></td>
                <td></td>
        </tr>
        
            <tr>
            
                <th>Article</th>
                <td>{{ commentaire.article.titre }}</td>
                <td>{{ commentaire.date ? commentaire.date|date('Y-m-d H:i:s') : '' }}</td>
       
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
                <td><a href="{{ path('app_commentaires_delete', {'id': commentaire.id}) }}">SUPPRIMER</a></td>
            </tr>
     
          {# TODO supprimer commentaire #}
         
             {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
           
        {% endfor %}
        </tbody>
    </table>
     <input type="hidden" name="_csrf_token"
                                                value="{{ csrf_token('authenticate') }}"
                                            >

    {# <a href="{{ path('app_commentaires_index') }}">back to list</a>

    <a href="{{ path('app_commentaires_edit', {'id': commentaire.id}) }}">edit</a> #}

    {# {{ include('admin/commentaires/_delete_form.html.twig') }} #}
{% endblock %}
